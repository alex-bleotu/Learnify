using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LevelSystem
{
    // private float experienceMultiplier = 25f;

    // public void AddExperience(User user, float experienceAmount) {
    //     float currentExperience = user.GetExperience();
    //     int currentLevel = user.GetLevel();

    //     currentExperience += experienceAmount;

    //     while (currentExperience >= currentLevel * experienceMultiplier) {
    //         Debug.Log("Level Up");
    //         currentExperience -= currentLevel * experienceMultiplier;
    //         currentLevel++;
    //     }

    //     user.SetExperience(currentExperience);
    //     user.SetLevel(currentLevel);
    // }
}
